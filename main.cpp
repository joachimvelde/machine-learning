#include "matrix.h"

int main()
{
    
    Matrix x(3, 3);
    // Matrix y(3, 3);

    // x.fill(3.1415927);
    // y.fill(2.7);

    // x.print();
    // y.print();
    // (x * y).print();

    return 0;
}
